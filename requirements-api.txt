# API deploy image deps (no Streamlit — that's the retired app.py).
# CPU-only torch on Linux; sentence-transformers loads torchvision at import.

--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.139.2
uvicorn==0.49.0
neo4j==6.2.0
anthropic==0.111.0
python-dotenv==1.2.2
requests==2.34.2

chromadb==1.5.9
sentence-transformers==5.6.0
transformers==5.12.1
torch==2.12.1+cpu
torchvision==0.27.1+cpu
