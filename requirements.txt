# Deps for the Streamlit redirect stub ONLY (app.py forwards to the Vercel app).
# Kept to just Streamlit so the redirect deploy boots fast and can't hang on the
# torch/ML stack.
#
# For LOCAL DEVELOPMENT of the real app (FastAPI + eval + pipelines), install
# requirements-local.txt instead. The deployed API image uses requirements-api.txt.

streamlit==1.58.0
